# API server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# NLP
underthesea>=6.8.0          # Vietnamese tokenizer (optional, fallback to whitespace)
langdetect>=1.0.9

# Embeddings (optional — swap with any encoder)
sentence-transformers>=2.6.0

# Database
# SQLite is built-in — no extra package needed

# Utilities
numpy>=1.26.0
scipy>=1.12.0
python-dotenv>=1.0.0
